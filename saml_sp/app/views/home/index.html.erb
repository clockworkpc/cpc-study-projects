<div class="container mx-auto h-full px-6">
  <div class="flex h-full items-center flex-col sm:flex-row">
    <div class="flex-1 flex items-center">
      <% if current_user %>
        <div>
          <h1>Congratulations, <%= current_user.name %>! You are inside the IdP.</h1>
        </div>
        <div class="flex-1">
          <img class="ml-auto" src="<%= asset_pack_path 'media/images/home-1.svg' %>" />
        </div>
      <% else %>
        <div>
          <h1 class="text-4xl mb-4">Lizard SAML IdP for Chameleon SPs</h1>
          <p class="text-lg text-gray-800 w-full sm:w-2/3">Powered by <i>Rails 6</i> and the <i>saml_idp</i> Gem.</p>
          <div class="py-6">
            <a class="button button-primary" href="/users/sign_up">Sign up now</a>
          </div>
        <% end %>
        </div>
    </div>
    <div class="flex-1">
      <img class="ml-auto" src="<%= asset_pack_path 'media/images/home-1.svg' %>" />
    </div>
  </div>
</div>
